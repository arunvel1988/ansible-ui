---
- name: Respond to high CPU usage
  hosts: localhost
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: High CPU alert rule
      condition: event.payload.cpu > 80
      action:
        run_playbook:
          name: cpu_alert.yml
