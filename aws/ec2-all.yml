#########################################################
plugin: amazon.aws.ec2
regions:
  - ap-south-1
filters:
  instance-state-name: running
hostnames:
  - tag: Name
compose:
  ansible_host: public_ip_address
groups: []
cache: yes
cache_plugin: jsonfile
cache_timeout: 300
#################################################################

plugin: amazon.aws.ec2
regions:
  - ap-south-1
filters:
  instance-state-name: running
  instance-type: t3.medium
hostnames:
  - tag: Name
compose:
  ansible_host: public_ip_address
groups: []

############################################

plugin: amazon.aws.ec2
regions:
  - ap-south-1
  - us-east-1
filters:
  instance-state-name: running
hostnames:
  - tag: Name
compose:
  ansible_host: public_ip_address
groups: []

#################################################################

plugin: amazon.aws.ec2
regions:
  - ap-south-1
filters:
  instance-state-name: running
hostnames:
  - tag: Name
compose:
  ansible_host: private_ip_address
groups: []

####################################################

plugin: amazon.aws.rds_instance
regions:
  - ap-south-1

filters:
  db-instance-status: available
hostnames:
  - db_instance_identifier
compose:
  ansible_host: endpoint.address
groups: []
####################################################
